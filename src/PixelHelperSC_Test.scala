import java.awt.Dimension
import javax.swing.JPanel
/*
// #Sireum #Logika
import org.sireum._

def TestDoPixel(x:Int, y:Int): Unit = {
  val panel2 = new JPanel()
  panel2.setPreferredSize(new Dimension(320, 220))
  var UUT = new PixelHelper(panel2)
  UUT.doPixel(x,y)
  assert(panel2.contains(x,y))
}

def TestDoPixelFail(x:Int, y:Int): Unit = {
  val panel2 = new JPanel()
  panel2.setPreferredSize(new Dimension(320, 220))
  var UUT = new PixelHelper(panel2)
  UUT.doPixel(x,y)
  //Check that there is no pixels around
  assert(!panel2.contains(x,y+1))
  assert(!panel2.contains(x,y-1))
  assert(!panel2.contains(x+1,y))
  assert(!panel2.contains(x-1,y))
  assert(!panel2.contains(x+1,y+1))
  assert(!panel2.contains(x-1,y-1))
  assert(!panel2.contains(x+1,y-1))
  assert(!panel2.contains(x-1,y+1))
}


*/

